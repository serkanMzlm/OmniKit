cmake_minimum_required(VERSION 3.1)
project(drivers)

set(COM_LIBRARY_DIR ${CMAKE_SOURCE_DIR}/communication/include)
set(COM_SOURCE_DIR ${CMAKE_SOURCE_DIR}/communication/src)

file(GLOB_RECURSE LIB_FILE ${COM_SOURCE_DIR}/*.c)

add_library(comm_lib ${LIB_FILE})
add_executable(mpu6050)

target_link_libraries(mpu6050 comm_lib)